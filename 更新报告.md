# 更新报告

本文件按时间倒序记录影响用户操作、数据格式、正式工作流或同步边界的实质维护；只在顶部新增条目，不覆盖、不删除历史记录。

## 2026-07-19：最终精简说明与固定 Git 操作顺序

### 改了什么

- 将用户操作统一为 Pull、Codex 修改与检查、只暂存白名单文件、Commit、Push。
- 精简 README 中的口令式示例、内部发布细节和代码页解释；AGENTS 改为按改动范围明确列出所需检查，只有实质维护才新增更新记录。
- 删除 `.gitignore` 中已被全局规则覆盖的 6 条缓存规则，并清理本次真实 PDF 冒烟测试产生的本地临时目录。
- 未修改 DeepPaperNote 工作流、脚本、测试、论文笔记、图片、导航、Base 或 GitHub Actions。

### 验证结果

- 被精简的缓存、`.local/`、`.obsidian/` 和 PDF 忽略规则仍然生效；跟踪文件中没有 PDF、数据库、密钥或本机状态。
- README 本地链接、导航确定性检查和严格 Vault lint 通过；Vault 为 9 篇笔记、39 张图片、导航覆盖 9/9、0 error、0 warning。
- `git diff --check` 通过；未执行暂存、Commit 或 Push。

## 2026-07-19：GitHub 同步改为用户手动操作

### 改了什么

- 将 GitHub 同步职责统一改为由用户在 Codex 侧边栏手动完成 Pull／Sync、暂存、Commit 和 Push。
- Codex 只负责修改、测试、导航检查、Vault lint、Git 文本检查和状态报告，不再执行 `git add`、`git commit` 或 `git push`。
- README、AGENTS 与 DeepPaperNote skill 使用同一规则；论文生成、发布和 Vault 校验流程不变。

### 验证结果

- 活跃工作流说明已统一，不再要求 Codex 询问后代为同步。
- 导航确定性检查、严格 Vault lint 与 Git 文本检查通过；没有改动论文正文、图片或权限配置。

## 2026-07-19：修复论文身份并精简 Vault 入口

### 改了什么

- 标题归一化改为支持 Unicode，规范 DOI/arXiv 身份；同题同年但 DOI 或 arXiv 冲突的候选不再自动合并，而是按歧义停止。
- 发布器从当前 paper record、evidence 和图片 manifest 重建并逐字段核对完整 synthesis；管线失败时向上保留截断、OCR 或证据缺失等具体原因。
- Git 白名单收紧到导航、Base、每篇论文的 `笔记.md` 和同级图片；同步前增加导航与 Vault 校验。
- 论文导航改成按年份排列、每篇论文只出现一次的简洁列表；主题筛选继续由 Base 提供，并删除正式流程不会产生内容的“待复核”视图。
- README 不再重复 Git 命令，删除一份重复图片回归测试；Zotero 可选通道保持不变。

### 验证结果

- Miniconda `deeppapernote` 环境下 Ruff 通过，完整 209 项 pytest 全部通过。
- 使用真实 7 页本地 PDF 完成离线管线，并通过新的完整 synthesis 绑定检查；临时运行目录已清理。
- 导航 `--check` 通过；严格 Vault lint 为 9 篇笔记、39 张图片、导航覆盖 9/9、0 error、0 warning。
- Git 白名单正反例、`git diff --check` 与工作区边界检查通过；没有 PDF、密钥、本机状态或临时文件进入改动。

## 2026-07-19：DeepPaperNote 最终可靠性复核与工作流收口

### 改了什么

- 将正式流程收口为确定性管线、独立文字/视觉复核和一次发布事务；发布器内部完成最终文件复核、导航原子重建、严格 Vault lint 与审计归档，失败时恢复旧论文目录和导航。
- 发布门禁重新验证每份本地 PDF 的读取前后 SHA-256、实际页数、逐文档 OCR 覆盖和论文类型所需证据；evidence、完整 synthesis、note plan、复核、frontmatter 与 passing lint 通过内容指纹和语义字段相互绑定。
- 图表资产重新计算稳定身份并绑定 paper record 的 document/page；contact sheet 绑定最终 decisions，正文只允许已插入且通过视觉复核的本地图片，拒绝远程、data URI、HTML 和引用式图片。
- 加固同名论文目录、run ID、审计复用、backup/report 路径、发布准备残留和发布后 note/image 哈希；offline 不再触发 DOI 元数据网络请求，direct PDF URL 必须从文档元数据或首页确认题名。
- 删除重复的 `create_paper_record_v2.py` 与旧 `figure_contracts.py`，合并显式 paper-record 入口、figure 契约和 JSON/hash 公共实现；删除 publisher dry-run、Vault lint no-fail 与重复 CI 测试步骤。正式 CLI 保留 16 个，内部脚本与 core 共 23 个。
- 正式目录无可靠图片时省略空 `images/`，并让 Vault lint 全局拒绝 `Research/` 下无笔记目录、发布临时目录、额外文件、损坏图片和孤儿图片。

### 验证结果

- Miniconda `deeppapernote` 环境为 Python 3.12.13、UTF-8 mode 1、PyMuPDF 1.28.0；`pip check` 无依赖冲突。
- Ruff、skill quick validate、Git 文本检查和完整 211 项 pytest 全部通过；保留 CLI 的 `--help` 与真实 PDF 离线管线包含在回归测试中。
- 独立终审先用最小反例发现 evidence、figure、review、发布与 Vault 边界问题；修复后逐项复验，最终确认无剩余 P1/P2。
- 导航 `--check` 通过；严格 Vault lint 为 9 篇笔记、39 张图片、导航覆盖 9/9、0 error、0 warning。跟踪文件中无 PDF、数据库、密钥、本机状态、缓存或权限维护文件。

### 已知问题或后续

- `.local/`、`.obsidian/` 与 `文献/` 继续只保留在本机，不进入 Git。
- 本次尚未执行 `git add`、commit 或 push；等待用户单独确认是否同步 GitHub。

## 2026-07-19：明确本地写入与临时授权边界

### 改了什么

- 明确 `Research/` 和普通项目文件在工作区写入权限下可按流程直接写入，`.agents/`、`.git/` 与 `.codex/` 继续保持受保护状态。
- 维护 DeepPaperNote 确需修改 `.agents/skills/DeepPaperNote/` 时，主代理必须先整理完整改动，再主动申请一次仅限本次批次和该子目录的临时写入批准；范围变化时必须重新申请。
- 禁止用永久 ACL、全局写入 profile、开放整个 `.agents/` 或新增权限脚本绕过保护；未修改 README、DeepPaperNote 工作流或论文笔记。

### 验证结果

- 权限规则仅写入根目录 `AGENTS.md`，项目内未新增权限配置、脚本或临时目录。
- Git 文本检查通过，跟踪区改动仅包含 `AGENTS.md` 与本条更新记录。

## 2026-07-19：统一 Miniconda 与 UTF-8 运行环境

### 改了什么

- 将 Windows 本地脚本、测试和校验统一到 Miniconda 环境 `deeppapernote`，明确使用 `conda run --no-capture-output` 顺序执行，避免混用裸 `python`、裸 `pip` 和临时环境。
- 在 README、AGENTS 和 DeepPaperNote skill 中统一记录首次配置、运行入口及环境门禁。
- 环境固定启用 Python UTF-8 mode；无需再逐次添加 `-X utf8`，也不依赖 Windows 活动代码页选择 Python 文本编码。

### 验证结果

- 环境使用 Python 3.12.13，PyMuPDF、pytest、Ruff 等项目依赖可用，`pip check` 无依赖冲突。
- DeepPaperNote skill 结构校验与 Git 文本检查通过。

## 2026-07-18：简化 DeepPaperNote 说明用语

### 改了什么

- 简化当前项目说明和 DeepPaperNote 工作说明中的工程表达，统一使用“内容指纹、安全发布、强制检查、中间文件、本地处理记录、待发布目录”等更直白的中文。
- 明确正常的一次性交付必须由主代理显式调用子 agent 完成独立复核，禁止主代理代写；只有用户明确选择时才改用人工复核。
- 保留 JSON 字段、脚本参数、文件名、状态值和错误码，避免影响现有程序、测试及本地发布记录。
- 历史更新条目保持原样，正式论文笔记不作改写。

### 验证结果

- DeepPaperNote skill 结构校验通过，Python 编译检查通过，Ruff 检查通过，151 项 pytest 全部通过。
- Vault 检查结果为 9 篇笔记、39 张图片、0 个错误、0 个警告，论文导航覆盖 9 篇。

## 2026-07-18：DeepPaperNote 可靠性加固与二次精简

### 改了什么

- 标题检索按 DOI、arXiv、规范标题与年份去重；多重可信身份、环境缺失、任一 PDF 解析失败、全文截断、OCR 覆盖不足或关键证据缺失均改为失败关闭，不再发布摘要型或 degraded 笔记。
- note plan 收敛为精确九字段结构，所有关键条目强制绑定存在的 evidence ID；文字复核强制记录作者、独立审阅者与来源，发布器拒绝自审、低分、遗留问题、过期哈希、非 polished 笔记和 staging 污染。
- lint 新增逐条关键结论页码、重复英文题名、多个 H1、数学环境外裸 LaTeX 与失效页内链接检查；发布器自动推导并核对 evidence level 和 figure status，审计替换失败时保留旧快照。
- 修复 input-record 路径静默忽略 CLI 补充材料的问题，并增加真实 PDF 离线回归；删除旧环境入口、工作区 fallback、旧目录路由、旧自动写作与图片包装逻辑及只服务于这些逻辑的测试。
- 用原 run ID 和本地主文/SI 重新跑三篇受影响笔记，修复重复英文题名、裸数学命令、页码与导航锚点、Universal 理论微分增益数值；独立视觉审阅发现并促成 Fig. S6 无污染重裁。

### 验证结果

- Python compile、Ruff 与 skill quick validate 全部通过；17 个保留 CLI 的 `--help`、20 项真实 PDF/发布集成测试和完整 151 项 pytest 全部通过。
- 三篇重跑均登记主文与补充材料，paper/evidence 状态为 pass；质量、可读性和 10 张受影响插图均由不同于作者的 Codex 审阅任务独立复核并绑定最终哈希。
- 9 篇笔记、39 张图片、导航覆盖 9/9，Vault lint 为 0 error、0 warning；9 份当前审计的规范文本、文件字节、图片与 artifact 列表全部匹配。
- Python 脚本由 10,705 行降到 9,529 行，净减少 1,176 行；`git diff --check` 通过，CI 已配置 Ubuntu Python 3.10/3.12 与 Windows Python 3.11。

### 已知问题或后续

- `.local/`、`.obsidian/` 与 `文献/` 继续只保留在本机，不进入 Git。
- 本次尚未执行 `git add`、commit 或 push；等待用户单独确认是否同步 GitHub。

## 2026-07-18：DeepPaperNote schema v2 单入口精简

### 改了什么

- 将正式流程收敛为 `run_pipeline_v2.py`、文字与图像复核、`publish_note_v2.py`、导航重建和 Vault lint；最终门禁合并回对应 v2 实现，删除 final、release、canonical、contract 包装层及旧 MVP、corpus、migration 入口。
- 将旧图片提取与 lint 实现改为私有 core 模块；证据、图片、lint、发布和 Vault 回归测试按子系统保留有效断言。
- 将 13 份重复 reference 收敛为 `workflow.md`、`writing.md`、`figures.md`、`vault.md`，并删除 skill 内双语 README、CHANGELOG、嵌套说明、嵌套 CI、营销图片和未使用模板。
- 正式论文目录只保留 `笔记.md` 与 `images/`；9 份 schema v2 审计归档到 `.local/deeppapernote/published/`，新增规范文本哈希、文件字节哈希和图片哈希快照后删除 `Research/*/manifests/`。
- 保留 9 篇论文正文、39 张图片、`文献/` PDF、`.obsidian/`、导航与 Bases；清除历史运行、副本、备份、缓存、egg-info、重复 CI 和临时补丁，合计回收约 658 MiB。

### 验证结果

- 全目录 Ruff 通过；完整 pytest 为 140 项通过，另有 5 条 PyMuPDF/SWIG 弃用警告。
- skill `quick_validate.py` 通过，18 个保留 CLI 的 `--help` 全部通过。
- 以本地 6 页 PDF 完成离线管线冒烟测试，证据、图片、3 张图表、synthesis bundle 和 run manifest 全部通过；隔离 publisher dry-run 的 contact sheet、视觉复核和 2 个插图物化全部通过，未写入正式 Vault。
- 9 份审计的 lint、质量、可读性、规范文本哈希、文件字节哈希和 39 张图片哈希全部匹配当前文件。
- Vault lint 结果为 9 篇笔记、39 张图片、导航覆盖 9/9、0 error、0 warning；Git ignore 边界与 `git diff --check` 通过。

### 已知问题或后续

- 测试警告来自 PyMuPDF 的 SWIG 类型弃用信息，不影响测试结果。
- `.local/`、`.obsidian/` 与 `文献/` 继续只保留在本机，不进入 Git。
- 本次未执行 `git add`、commit 或 push；等待用户单独确认是否同步 GitHub。

## 2026-07-18：项目说明与维护规则清理

### 改了什么

- 将 README 收敛为论文来源、生成笔记、保存到 Research 和使用 Obsidian 阅读的简明说明。
- 将更新报告改为倒序累计记录，并把维护规则写入 AGENTS。
- 把 `更新报告.md` 加入 Git 允许范围和同步示例。
- 删除会修改根目录文档的三个一次性迁移脚本及其对应测试，避免维护文案再次被自动写入 README。

### 验证结果

- README 不再包含单次更新、内部流程说明或本机绝对路径。
- README 中的本地链接均指向现有文件，`更新报告.md` 已纳入 Git 同步白名单。
- DeepPaperNote 的 Ruff 检查和 Vault lint 通过；Vault lint 检查了 9 篇笔记和 39 张图片，结果为 0 error、0 warning。
- `git diff --check` 通过。
- 当前 Python 环境未安装 pytest，因此未执行完整测试集；本次没有为维护文档额外安装依赖。

### 已知问题或后续

- 本次未改动核心论文处理管线，也未整理 `.local` 中的历史运行数据。
- `更新报告.md` 需要在下一次经用户确认的 GitHub 同步中首次纳入版本历史。

## 2026-07-16：论文笔记可读性清理

### 改了什么

- 复核并整理了 9 篇论文笔记，移除 49 个选图或质检过程块和 147 处流程字段。
- 正文保留 39 张真实图片，并把说明改为适合阅读的自然图注。
- 修复了 1 处图片状态不一致，并统一了标题、速览、技术章节、导航和论文库入口。
- 没有可靠图片的段落继续用文字解释，不保留占位框或后台状态说明。

### 验证结果

- 9 篇笔记全部通过内容、可读性、图片和 Vault 校验。
- Vault lint 检查了 9 篇笔记和 39 张图片，结果为 0 error、0 warning。
- 当时的自动化测试为 161 项通过，另有 5 条来自 PyMuPDF/SWIG 的弃用警告。
- 39 张已发布图片均存在且可解码，未发现坏链接、绝对路径或临时目录引用。

### 已知问题或后续

- 10 张未达到清晰度或完整性要求的图片没有写入笔记；笔记正文不依赖这些图片即可阅读。
